{
  "name": "codecrypto-wallet",
  "version": "1.0.0",
  "description": "Chrome Extension Wallet con soporte completo para Ethereum",
  "scripts": {
    "build": "echo 'Compilación manual requerida'",
    "dev": "echo 'Modo desarrollo no disponible'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.251"
  }
}
